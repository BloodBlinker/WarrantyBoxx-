<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Blueprint Section 6.2: permissions requested only at point of use. -->
    <!-- Camera: requested at runtime when user taps "Take photo". -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Gallery access. READ_MEDIA_IMAGES on API 33+, READ_EXTERNAL_STORAGE below. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Notifications: runtime request on API 33+ after first item added. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Re-deliver missed reminders after device reboot (no runtime request). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Exact alarms for precise warranty-expiry reminders. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Keep WorkManager jobs alive for background widget/reminder refresh. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Camera hardware is optional; app is fully usable without it. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:label="WarrantyBoxx"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep links from notifications: warrantyvault://item/{id} (Section 5.4). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="warrantyvault" android:host="item" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Home screen widget (Blueprint Section 2.1, home_widget package). -->
        <receiver
            android:name=".WarrantyWidgetProvider"
            android:exported="true"
            android:label="WarrantyBoxx">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/warranty_widget_info" />
        </receiver>

        <!-- Background callback used by home_widget for interactivity. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
